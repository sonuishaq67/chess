# ~512M param config (Chinchilla-optimal for 10B tokens)
d_model: 1536
n_layers: 18
n_heads: 24         # head_dim = d_model / n_heads = 64
ffn_dim: 6144       # 4 × d_model
vocab_size: 1972
max_seq_len: 256
dropout: 0.1
